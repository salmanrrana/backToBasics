// "history" was automatically added with "react-router-dom"
// from history package we are calling createBrowserHistory

// This is all it takes for us to create our History Object that we can maintain
// also make sure to have a "plain" router for our routing
// instead of a browserRouter

import createHistory from "history/createBrowserHistory";

export default createHistory();
